# Core dependencies (mirrors pyproject.toml [project.dependencies])
click>=8.1
lxml>=5.0
pydantic>=2.6
pysolr>=3.9
python-dateutil>=2.8
PyYAML>=6.0
tqdm>=4.66

# Optional: colored console output (pip install .[colored])
# coloredlogs>=15.0

# Optional: WMS thumbnail generation (pip install .[wms])
cartopy
matplotlib
OWSLib
pykdtree

# Optional: solr_id resolution from solrindexer (pip install .[solrindexer])
solrindexer @ git+https://github.com/metno/solr-indexer.git@feature/bulkindex-pipeline
